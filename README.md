# ArduinoUsingRaspi
## Program Arduino using command line
On Linux, you need to install avr-gcc (package is usually called gcc-avr), avr-libc and avrdude. On Debian systems the command
```
sudo apt-get install gcc-avr avr-libc avrdude
```
can be used to install the required packages.
